%h1 Listing batches

%table
  %tr
    %th Year
    %th Remarks
    %th
    %th
    %th

  - @batches.each do |batch|
    %tr
      %td= batch.year
      %td= batch.remarks
      %td= link_to 'Show', batch
      %td= link_to 'Edit', edit_batch_path(batch)
      %td= link_to 'Destroy', batch, :confirm => 'Are you sure?', :method => :delete

%br

= link_to 'New Batch', new_batch_path
