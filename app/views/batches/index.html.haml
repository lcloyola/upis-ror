%h1 Listing batches

%table
  %tr
    %th Year
    %th Remarks
    %th
    %th
    %th

  - @batches.each do |batch|
    %tr
      %td= batch.year
      %td= batch.remarks
      %td= link_to 'Show', batch_students_path(batch)
      %td= link_to 'Edit', edit_batch_path(batch)
      %td= link_to 'New Student', new_batch_student_url(batch)

%br

= link_to 'New Batch', new_batch_path
